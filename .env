# Base URL da API Laravel (incluir /api)
# Ex.: https://seudominio.test/api
# Em dev com proxy Vite, use: /api e defina o alvo abaixo
VITE_API_URL=http://localhost:8000/api

# Alvo do proxy em dev (Laragon / backend) — só usado pelo Vite
# VITE_DEV_PROXY_TARGET=https://seudominio.test
